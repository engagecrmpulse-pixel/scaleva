import { QuizClient } from "./QuizClient";

export const metadata = {
  title: "Get started — Scaleva",
  description: "Tell us about your business and we'll build your personalized setup in 2 minutes.",
};

export default function QuizPage() {
  return <QuizClient />;
}
